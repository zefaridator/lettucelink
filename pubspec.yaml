name: lettucelink
description: "Social network for gardeners"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0-140.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  open_meteo: ^1.0.5
  water_drop_nav_bar: ^2.2.0+5
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
